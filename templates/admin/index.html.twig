{% extends 'admin/layout.html.twig' %}
{% block title %}Administration{% endblock title %}

{% block admin_content %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 d-lg-none my-3 d-flex justify-content-end">
                <button class="btn" data-toggle="collapse" data-target="#sidebar">
                    <i class="fas fa-bars"></i>
                </button>
            </div>

            <nav id="sidebar" class="sidebar collapse d-lg-block col-12 col-lg-3 col-xl-2">
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_index') }}">Accueil</a>

                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_product-editor') }}">Ajouter un produit</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_product-index') }}">Tous les produits</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_order-index') }}">Commandes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_atelier-editor') }}">Ajouter un Atelier</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_atelier-index') }}">Tous les Atelier</a>
                    </li>
                </ul>
            </nav>

            <main class="col-lg-9 col-xl-10 py-lg-4 py-md-3 py-2">

            </main>
        </div>
    </div>

    <header class="mb-4">
        <h1>Administration</h1>
    </header>
    <p>{{ product_rows }} produits en vente</p>
    <p>{{ total_revenue }} € de chiffre d'affaires</p>

{% endblock %}
