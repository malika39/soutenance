{% extends 'shop/layout.html.twig' %}
{% block title %}Administration{% endblock title %}

{% block content %}

    <div class="container-fluid">

        <div class="row">
            <div class="col-12 d-lg-none my-3 d-flex justify-content-end">
                <button class="btn" data-toggle="collapse" data-target="#sidebar">
                    <i class="fas fa-bars"></i>
                </button>
            </div>



            <nav class="navbar navbar-expand-lg navbar-light bg-light">

                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">

                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('admin_product-editor') }}">Ajouter un produit <span class="sr-only">(current)</span></a>
                        </li>
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('admin_product-index') }}">Tous les produits <span class="sr-only">(current)</span></a>
                        </li>
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('admin_order-index') }}">Commande <span class="sr-only">(current)</span></a>
                        </li>
                    </ul>
                </div>
            </nav>




            <main class="col-lg-9 col-xl-10 py-lg-4 py-md-3 py-2">
                {% block admin_content %}

                {% endblock %}
            </main>
        </div>
    </div>
    <header class="mb-4">
        <h1>Administration</h1>
    </header>
    <p>{{ product_rows }} produits en vente</p>
    <p>{{ total_revenue }} € de chiffre d'affaires</p>

{% endblock %}
