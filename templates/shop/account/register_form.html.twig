{% extends 'shop/layout.html.twig' %}


{%  block content %}
<div class="container" id="form-welcome2">
    <h1 class="welcome text-center">Confirmer l'inscription</h1>
{{ form_start(registrationForm) }}
    {{ form_row(registrationForm.email, {
        'label': 'E-mail',
    }) }}
    {{ form_row(registrationForm.plainPassword.first, {
        'label': 'Mot de passe',
    }) }}
    {{ form_row(registrationForm.plainPassword.second, {
        'label': 'Confirmer le mot passe',
    }) }}

    {% if order == true %}
        <input type="hidden" name="_target_path" value="{{ path('checkout_address') }}" />
    {% endif %}

    <input type="submit" value="Créer mon compte" class="btn btn-primary button-form">
{{ form_end(registrationForm) }}
</div>
{% endblock %}

